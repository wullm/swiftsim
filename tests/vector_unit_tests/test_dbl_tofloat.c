#include "vector_tests.h"

int main(){

printf("test_dbl_tofloat NYI\n");
return 0;

/*vector v1, v2;
for(int i = 0; i < VEC_SIZE; i++){
v2.f[i] = 0.0f;
}
  for(int i = 0; i  < VEC_SIZE/2; i++){
       v1.d[i] = (double)(i+1);
  }
  vec_dbl_tofloat(v1.vd, v2.v);
  for(int i =0; i < VEC_SIZE; i++){
    if(i < VEC_SIZE/2 && v2.f[i] != (float)(i+1)){
        return 1;
    }else if( i >= VEC_SIZE/2 && v2.f[i] != 0.0){
        return 1;
    }   
  }


    return 0;*/
}


